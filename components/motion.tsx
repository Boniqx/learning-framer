import { Box, Flex, HStack, Skeleton, SkeletonCircle, Stack, Text, VStack } from '@chakra-ui/react';
import { motion } from 'framer-motion';

export const MotionBox = motion(Box);
export const MotionHStack = motion(HStack);
export const MotionText = motion(Text);
export const MotionVStack = motion(VStack);
export const MotionFlex = motion(Flex);
export const MotionStack = motion(Stack);
export const MotionSkeleton = motion(Skeleton);
export const MotionSkeletonCircle = motion(SkeletonCircle);
